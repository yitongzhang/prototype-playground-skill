/**
 * Master prototype — pixel-perfect clone of the production main screen.
 * This placeholder is replaced during playground setup. Once built, this
 * directory is frozen: iterations copy it, they never edit it.
 */
export default function Master() {
  return (
    <div style={{ padding: 48, fontSize: 14, color: '#666' }}>
      Master prototype not built yet. Replace src/prototypes/master/ with the
      production main-screen clone.
    </div>
  )
}
